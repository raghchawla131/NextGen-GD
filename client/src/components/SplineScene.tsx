import Spline from '@splinetool/react-spline';
import robotScene from '../spline/scene (2).splinecode'

export default function SplineScene() {
  return (
    <Spline scene={robotScene} className='' />
  );
}
